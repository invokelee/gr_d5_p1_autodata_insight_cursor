# Vercel (Production / Preview): set in Project Settings → Environment Variables.
OPENAI_API_KEY=sk-...

# Optional: override default model if your account uses a different slug.
OPENAI_MODEL=gpt-5-mini

# Optional: limit EDA-heavy column fan-out on wide tables (Mercedes-scale).
EDA_MAX_COLUMNS=80
